; Emu68-Tools Installer

; Generated by InstallerGen 1.5!

(complete 0)
(set @app-name "Emu68-Tools Installer")
(set @default-dest "SYS:")
; 
(welcome "This installer will copy all necessary driver files to it`s standard locations. Additional tools will be installed to a location of your choice.")

(complete 2)
; 
(set #RTG-exists
  (exists "LIBS:Picasso96/rtg.library" (noreq))
)

(complete 5)
; 
(if (= #RTG-exists 1)
(

(complete 10)
; 
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest "LIBS:Picasso96")
  (choices "VideoCore/VideoCore.card" "VideoCore/Emu68-VC4.card")
)

(complete 15)
; 
) ; Else
(

(complete 100)
; 
(message "Picasso96 not found. Please install Picasso96 first. You can find it on Aminet or buy the newest Version from Individual Computers - icomp.de."
)

)
) ; End If
(complete 30)
; 
(set #TCPIP-exists
  (exists "LIBS:bsdsocket.library" (noreq))
)

(complete 40)
; 
(if (= #TCPIP-exists 1)
(

(complete 50)
; 
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest "DEVS:Firmware")
  (choices "Emu68-WiFi/Devs/Firmware")
)
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest "Devs:Networks")
  (choices "Emu68-WiFi/Devs/Networks/wifipi.device")
)

; 
) ; Else
(

(complete 100)
; 
(message "TCP/IP Stack not found. Please install a TCP/IP Stack first. You can use Roadshow or Miami."
)

)
) ; End If
(complete 80)
; 
(set #default-dest-installergen-temp
  (askdir
    (prompt "Where to install additional Tools? A folder called \"Emu68-Tools\" will be created. Command line tools will be copied to C:")
    (help @askdir-help)
    (default @default-dest)
  )
)
(set @default-dest #default-dest-installergen-temp)

(complete 90)
; 
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest (tackon @default-dest "Emu68-Tools"))
  (choices "EmuControl/EmuControl" "EmuControl/EmuControl.info")
)
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest "C:")
  (choices "I2C/SetClockI2C" "Buddhabrot/Buddhabrot" "LatticeI2cProg/LatticeI2cProg" "SmallPT/SmallPT" "VC4Tweak/VC4Tweak")
)
(copyfiles
  (prompt "")
  (help @copyfiles-help)
  (source "")
  (dest (tackon @default-dest "Emu68-Tools"))
  (choices "Contrib/Emu68Info_0.1.7.lha" "Contrib/Emu68Reset_0.1.1.lha")
)

(complete 100)
; 
(message "Installation complete. You`ll find all Tools in your choosen location."
)

